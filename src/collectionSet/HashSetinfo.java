package collectionSet;

import java.util.HashSet;

public class HashSetinfo {
	
	//Iterable->Collection->Set->HashSet;

	public static void main(String[] args) {
		
   //child interface of collection 
//group of individual object where duplicate are not allowed and insertion order is not preserved;
		//heterogenous object are allowed 
		//null is allowed 
		//Serializable and clonable 
		//search operation the is the best choice for hash set 
		//hash code is originated
		//underline data structure is hashTable
		//Default initial capacity is 16
		//fill ratio or Load factor 0.75
		
		HashSet<String > h =new HashSet<String>();
	//	HashSet hs=new HashSet(int 100);//
	//	HashSet hs=new HashSet(int 100,flot );

		
	}

}
